// TODO: 스토리지 보안 규칙 구현
// 구현 명세: docs/03-domain-models.md

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 에뮬레이터 테스트용: 모든 접근 허용
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
