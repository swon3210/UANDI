# 보관함 (archive)

블로그에 **발행하지 않는** 지난 글들을 모아둔 곳이다.

- 블로그 앱(`apps/blog`)은 `content/posts/`만 읽는다. 이 폴더의 글은 빌드·목록·RSS 어디에도 나타나지 않는다.
- 지우지 않고 옮겨둔 이유는, 나중에 다시 쓸 때 소재와 문장을 참고하기 위해서다.
- 다시 발행하려면 파일을 `content/posts/`로 옮기고 frontmatter(`category` 필수)를 확인한다.
