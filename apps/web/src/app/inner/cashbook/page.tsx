'use client';

import { Dashboard } from '@/components/dashboard/Dashboard';

/** 가계부 탭의 루트 — 예산 요약 대시보드. 여기서 내역/캘린더/결산/계획으로 드릴다운한다. */
export default function CashbookHomePage() {
  return <Dashboard />;
}
